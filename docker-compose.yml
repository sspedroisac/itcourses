services:
  app:
    image: node:22-alpine
    container_name: itcourses
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    command: sh -c "yarn install && yarn dev --hostname 0.0.0.0"
